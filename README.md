# Hatuli — аналитическая платформа инвестиций в недвижимость Астаны

Система автоматически собирает данные об объявлениях продажи и аренды квартир,
считает для каждого объекта многофакторный Deal Score, строит тепловые карты
города (цены, аренда, шум, транспорт, хайп), анализирует новости недвижимости,
обогащает данные о ЖК/застройщиках из официальных реестров и помогает принять
решение о покупке — с локальным (гексагональным) сравнением цены, анализом
торга по реальным аналогам и рейтингом ЖК/застройщиков.

**Хранилище данных — PostgreSQL** (единственный канонический источник; SQLite
`bot.db` — легаси от Telegram-бота, не используется). Веб-панель на FastAPI,
фоновые systemd-сервисы, кроны оркестратора Hermes.

---

## Что умеет

### Аналитика объявлений
- **Сбор данных о продаже и аренде** — квартиры (комнатность, площадь, этаж,
  цена, координаты, фото-ссылки, описание), история изменения цены, признак
  ухода объявления в архив. Синхронизация из Krisha через systemd-сервисы
  (продажа 30–120 мин, аренда 5–15 мин).
- **Deal Score v4** — единый скор 0–100 по пяти факторам (цена vs локальный
  рынок, локация+инфраструктура, качество ЖК, доходность/ликвидность, риск),
  с «уверенностью» — какая доля факторов посчитана на реальных данных.
- **Гексагональный анализ цены** — ожидаемая цена/м² строится из медианы по
  своему гексагону + 6 соседних + городу (с учётом класса ЖК), а не по всему
  району целиком.
- **Анализ торга** — сравнение с реальными аналогами (гексагон+кольцо,
  комнаты, площадь ±15%, класс ЖК), с честной пометкой при нехватке данных.
- **Классификация отделки** — черновая / с отделкой / с мебелью по тексту
  объявления (эвристика; опциональный платный LLM-анализ).
- **Реальные просмотры** — `krisha-viewcount.service` (Playwright/headless
  Chromium): считывает число просмотров объявления (статичный HTML Крыши
  число не отдаёт, нужен JS/фингерпринт) и копит историю, цикл ~1 ч.
- **Дедупликация, геопривязка, проверка актуальности** (архив).

### ЖК и застройщики
- **Обогащение данными о ЖК/застройщиках** — класс жилья, теххарактеристики,
  застройщик — из Korter.kz, Homsters.kz, Krisha. Интервал обхода 5 дней
  (настраивается на вкладках парсеров).
- **Официальные данные homeportal.kz (реестр КЖК)** — `homeportal_scan.py`
  (щадящий: 1 ЖК / 90 с) тянет 619 объектов Астаны: этажность, число квартир,
  лифты, БИНы застройщика/технадзора/уполномоченной, даты строительства.
  Блок «Официальные данные» — на страницах ЖК и застройщиков.
- **Статус строительства** — для ЖК с карточкой Крыши сканер
  `krisha_complex_scan.py` вытаскивает `build_status` («Строящийся» /
  «Сдан в эксплуатацию») и `deadline` («Срок сдачи: II кв. 2026»).
- **Housing-class** — классификация ЖК с характеристиками: цена за м²,
  этажность, высота потолков, лифты (кол-во и тип), число квартир
  (таблицы `complex_tech_specs`, `housing_class_test`, ручной ввод на
  странице `/admin/analytics/complexes`).
- **Отслеживание изменений источников** — `source_changes`/`source_runs`:
  дифф новых/изменённых ЖК по каждому прогону Korter/Homsters (вкладки
  парсеров: изменения за 1/3/7/30 дней, длительность обходов).
- **Банки и ипотека** — 14 банков, 27 ипотечных программ
  (`/admin/banks`, `/admin/banks/{slug}`): ставки, ГЭСВ, взносы, условия,
  ссылки на источники.
- **Калькулятор ипотеки** (`/admin/mortgage-calculator`) — ежемесячный
  аннуитетный платёж по каждой подходящей программе (цена/ПВ/срок/тип жилья).
- **Оценка банка** — показатель в блоке ипотеки (большой попап карты,
  страница объявления). Пока заглушка = наша расчётная «реальная цена»
  (`bargain.target_price`), см. `/admin/info#bank-estimate`.

### Фото и планировки
- **Детекция планировок (SigLIP)** — `floorplan_scan.py`: гибрид OpenCV-эвристики
  (быстрый фильтр по насыщенности/белому/чёрным линиям) + SigLIP-классификатор
  (ViT-B-16, ~9 img/s на CPU), различает план квартиры от обычного фото.
  Найденные планы — блок «План квартиры» в карточке объявления, прогресс —
  на странице `/admin/analytics/photo-analysis`.
- **Чистка мусорных фото (SigLIP-эталоны)** — эталонные «мусорные» картинки
  (заглушки/реклама) сравниваются с фото объявлений (порог 0.82); найденные
  URL попадают в `blocked_photo_urls` и вычищаются из всех объявлений одним
  SQL (сделано для ~15 тыс. объявлений). Интегрировано в `floorplan_scan.py`.

### Карты города
- **Тепловые карты** на дашборде (13 режимов): цены продажи/аренды, цена за м²,
  доходность, внешний шум (OSM-дороги), транспортная доступность, хайп по
  новостям, плотность населения, количество объявлений, снос домов,
  инфраструктура, новизна домов и **преступность**.
- **Преступность** — официальный портал КПСиСУ ГП РК (`gis.kgp.kz`, слой
  KPSSU/crime): 250k+ записей по Астане с 2015 г., ежедневное обновление
  (`krisha-crime.timer` 06:30). Гексы 150 м: оттенок = тяжесть (0–4),
  непрозрачность = количество; в настройках карты — выбор периода
  **месяц / полгода / год**.
- **Транспортная доступность** — 5 409 гексов (ребро 100 м) со скорингом:
  ЛРТ-станции ≤1 км (вес 0.75) + автобусные остановки ≤500 м (вес 0.4).
- **Точки притяжения** — 75+ достопримечательностей и 24 точки ЛРТ
  (действующие станции + плановые линии с примерными сроками запуска).
- **Карта ЖК** (`/admin/complexes`) — все ЖК с подписями: название на синем
  фоне, число активных объявлений, класс жилья; пометки «Крыша/Homeportal/
  Объявления» — какие данные у ЖК есть.

### Хайп-аналитика по новостям (LLM)
- **Принцип**: хайп локаций строится **только из новостей** — LLM (DeepSeek)
  читает заголовки+выжимки статей и извлекает упоминания конкретных локаций
  (ЖК, станции ЛРТ, парки), присваивает рейтинг 0–100 и причину.
- **Геопривязка**: станция ЛРТ → все чистые ЖК в радиусе 500 м (рейтинг
  убывает с расстоянием); **парк в положительном контексте** (открытие/
  благоустройство/реконструкция) → ЖК в радиусе 700 м; иначе — точный ЖК
  по имени. ТРЦ-«парки» отсекаются.
- **Цикл**: сбор новостей 1×/день 08:30 (Google News RSS + Playwright),
  Threads — каждый час, медиа-скан СМИ — 5×/день (8/11/14/17/20),
  **LLM-анализ — 21:00** (`krisha-hype-news.timer`, флаг `AI_HYPE_NEWS`).
- **Хранение**: `hype_tracker` (отдельная БД) — `hype_resources`,
  `hype_snapshots`, `hype_resource_runs`, `hype_locations` +
  `hype_location_history`, `news_analysis_runs`.

### Новости
- Раздел `/admin/news` — ежедневный дайджест новостей недвижимости Астаны
  (только СМИ): карточки с фото, выжимка, страница новости `/admin/news/{id}`.
- **«Анализ новостей»** — графики: новости по дням, токены LLM-анализа.

### Гигиена данных (автоматическая)
- **Дедуп ЖК** — `complexes_cleanup.py`: нормализация имён (вкл. казахские
  буквы ә→а, ұ→у, қ→к), кластеры-дубли по координатам/имени, каноническая
  запись, перепривязка объявлений, `is_garbage`. Очищено ~900 мусорных ЖК.
- **Сопоставление с Крышей/homeportal** — `match_601.py`,
  `match_homeportal_531.py`: фаззи-матчинг названий (порог 0.85+, короткие
  имена — только точное) для проставления `krisha_url` и `matched_complex_id`.
- **Отвязка улиц** — `street_audit_daily.py` (cron 04:30): находит «ЖК»,
  где ≥60% объявлений имеют название ЖК в адресе (= улица), помечает
  `is_street`, отвязывает объявления, пересчитывает координаты.

### Прочее
- **Бэкапы** — `backup_hatuli.py` (локально на Windows): дампы
  krisha_bot + hype_tracker + архив проекта (без venv/.git/фото-кэша),
  ротация 5, ежедневно 03:00; каждая запись — в `backup_history` со
  страницей `/admin/backup` (график размеров по времени).
- **Google Sheets sync** — квартиры, аренда, инвест-объекты.
- **Telegram-алерты** (платные, Stars) — сервис `krisha-alerts`.

---

## Архитектура

```
krisha_bot/
├── bot/
│   ├── core/
│   │   ├── apartment_parser.py     # Сбор объявлений о продаже + pipeline
│   │   ├── apartment_details.py    # Детальная докачка страницы объявления
│   │   ├── deal_score.py           # Deal Score v4 — итоговый скор
│   │   ├── hex_price.py / hexgrid.py   # Гексагональный индекс цены, сетка
│   │   ├── bargain.py              # Анализ торга по гекс-аналогам
│   │   ├── finish_classify.py      # Классификация отделки по тексту
│   │   ├── ai_text_analysis.py     # Опциональный LLM-анализ текста
│   │   ├── site_enrichment.py      # Дифф изменений источников (source_changes)
│   │   ├── rebind.py               # Геопривязка/геокодинг
│   │   ├── rental_parser.py        # Сбор аренды + rental_index
│   │   ├── dedup_listings.py       # Дедупликация
│   │   ├── housing_class_score.py  # Оценка класса ЖК по характеристикам
│   │   ├── sheets_sync.py          # Google Sheets
│   │   └── archive_check.py        # Проверка ухода в архив
│   ├── db/
│   │   ├── pg.py                   # PostgreSQL connection pool (asyncpg)
│   │   └── models.py / schema.sql  # DDL / инициализация схемы
│   ├── admin_web.py                # FastAPI: дашборд, аналитика, hubs
│   └── templates/                  # Jinja2 (dashboard, аналитика, банки…)
├── hype_tracker/                   # Хайп-аналитика (отдельная БД hype_tracker)
│   ├── hype_collect.py             # Сбор 25 ресурсов, снимки 2×/день
│   ├── hype_annotate.py            # Аннотации прогонов
│   ├── news_collect.py             # Сбор новостей (Google News RSS + Playwright)
│   ├── news_analyze.py             # LLM-анализ новостей → hype_locations
│   ├── article_text.py             # Полные тексты статей для LLM-анализа
│   ├── media_scan.py               # Медиа-скан СМИ 5×/день
│   ├── krisha_catalog.py           # Каталог ЖК Крыши (complex/search) → krisha_complex_catalog
│   ├── krisha_complex_scan.py      # Скан карточек ЖК: квартиры, фото, статус, срок
│   ├── homeportal_scan.py          # Реестр КЖК (619 объектов Астаны)
│   ├── location_upsert.py          # Апсерт локаций хайпа
│   └── transport_hexes.py          # Расчёт гексов транспортной доступности
├── service_web.py                  # Веб-панель (FastAPI, порт 8082)
├── service_apartments.py           # Сервис сбора продаж
├── service_rental.py               # Сервис сбора аренды
├── service_viewcount.py            # Сервис реальных просмотров (Playwright)
├── service_geobind.py              # Геопривязка/докачка ЖК
├── service_korter.py / service_homsters.py  # Обогащение (5 дней ±6 ч)
├── service_market_data.py          # Рыночные справочные данные
├── service_alerts.py               # Telegram-алерты
├── service_site_bot.py             # Telegram-логин + дайджест избранного
├── complexes_cleanup.py            # Дедуп ЖК (по требованию/после правок)
├── match_601.py / match_homeportal_531.py  # Фаззи-сопоставление ЖК
├── street_audit_daily.py           # Авто-отвязка улиц (cron 04:30)
├── floorplan_scan.py               # SigLIP-сканер планировок + мусорные фото
├── terminal_extras.py              # API/страницы: хайп, банки, новости, ЖК, бэкапы
└── migrations/                     # SQL-миграции 001–036
```

---

## Базы данных

### krisha_bot (основная, 65 таблиц)

| Таблица | Описание |
|---------|----------|
| `apartment_listings` | Квартиры на продажу — данные, скоры, история цены |
| `rental_listings` / `rental_index` | Объявления аренды / агрегированные ставки |
| `complexes` | ЖК — класс, год, застройщик, цена/м², координаты, `build_status`, `deadline` |
| `complex_tech_specs` | Теххарактеристики ЖК: этажность, потолки, лифты, отопление, фасад |
| `housing_class_test` | Ручной ввод: лифты, квартиры, комнаты по типам (source: krisha/homeportal) |
| `developers` | Застройщики — репутация, БИН, сайт, проекты |
| `homeportal_objects` | Официальный реестр КЖК: 619 объектов, БИНы, этажность, квартиры, `matched_complex_id` |
| `krisha_complex_catalog` | Каталог ЖК Крыши: 1110 слагов Астаны (из sitemap complexes.xml) |
| `krisha_parse_log` / `homeportal_parse_log` | Логи обходов парсеров |
| `source_changes` / `source_runs` | Диффы изменений ЖК по прогонам Korter/Homsters |
| `backup_history` | История бэкапов (размеры, статус) — страница `/admin/backup` |
| `blocked_photo_urls` | Мусорные фото-URL (SigLIP-эталоны), вычищены из объявлений |
| `price_history` | История цены по объявлению |
| `news` | Новости недвижимости (СМИ): title, source, url, image_url, summary |
| `banks` / `mortgage_programs` | Банки и ипотечные программы (14/27) |
| `transport_stops` / `transport_hexes` | Остановки ОСМ (838) / гексы транспорта 100 м (5 409) |
| `city_roads` / `city_poi` | Дороги для карты шума / POI (школы, сады, landmark, ЛРТ) |
| `app_settings` | Настройки: веса скоринга, интервалы парсеров, флаги LLM |

### hype_tracker (хайп)

| Таблица | Описание |
|---------|----------|
| `hype_resources` | 25 ресурсов (market/news/blog/social/official) |
| `hype_snapshots` | Снимки 2×/день (ресурсы, объекты, summary) |
| `hype_resource_runs` | Прогоны ресурсов (Threads ежечасно, СМИ 5×/день) |
| `hype_locations` / `hype_location_history` | Локации хайпа: имя, район, координаты, рейтинг 0–100 + история |
| `news_analysis_runs` | Статистика LLM-прогонов: новостей, источников, токенов |
| `processed_articles` | Уже проанализированные новости (дедуп по URL) |

---

## Deal Score v4 (0–100)

| Компонент | Вес | Источник |
|-----------|-----|----------|
| Цена vs локальный рынок | 40% | Гексагон+кольцо+город, с поправкой на класс ЖК и потолки |
| Локация+инфраструктура | 20% | Цена гексагона vs города + POI рядом |
| Качество ЖК | 20% | Класс/год/рейтинг, либо перцентиль цены в сегменте |
| Доходность+ликвидность | 15% | Yield по rental_index + число аналогов в ЖК |
| Риск | 5% | Этаж (1й/последний), риелтор vs собственник |

Веса настраиваются в `/admin/settings`. Полное описание — вкладка «Инфо».

---

## Хайп-методика (кратко)

1. Сбор: новости (08:30) + Threads (ежечасно) + СМИ-скан (5×/день) + снимки (8:00/20:00).
2. LLM-анализ в 21:00 (`news_analyze.py`): определяет упомянутые локации и рейтинг 0–100.
3. Геопривязка: станция ЛРТ → ЖК в 500 м; парк (в хорошем смысле) → ЖК в 700 м;
   иначе — точный ЖК по имени (фаззи, казахская нормализация).
4. Запись в `hype_locations` + история → карта обновляется автоматически.
5. Отчёт в чат: N новостей из M источников (в т.ч. Threads K), токены, топ-3 локации.

Подробности — вкладка «🔥 Хайп» на странице `/admin/info`.

---

## Веб-панель (http://192.168.1.68:8082, публично: https://hatuli.ai-groundtruth.com)

```
/admin                        — карта, фильтры, тепловые карты (вкл. «Хайп (новости)»)
/admin/analytics              — квартиры со скорами, аналитика
/admin/analytics/prices       — цены
/admin/parsers                — hub парсеров: общие данные, Korter, Homsters, homeportal, комплекс-скан
/admin/analytics/complexes    — hub ЖК: «Класс жилья» (этажность/потолки/лифты/квартиры + пометки Крыша/Homeportal/Объявл.) и «ЖК под правку»
/admin/complexes              — карта ЖК с подписями (название, кв., класс)
/admin/developers             — рейтинг застройщиков (+официальные данные КЖК)
/admin/backup                 — история бэкапов и график размеров
/admin/banks                  — банки и ипотека (14 банков)
/admin/news                   — новости недвижимости (+ /admin/news/{id})
/admin/analytics/photo-analysis — SigLIP-планировки и мусорные фото
/admin/info                   — документация: скоринг, шум, хайп
/admin/settings               — веса Deal Score, лимиты сбора, монетизация
/admin/panel                  — админ-панель (все разделы)
```

---

## Сервисы (systemd)

| Сервис | Описание |
|--------|----------|
| `krisha-web` | Веб-панель FastAPI (порт 8082) |
| `krisha-apartments` | Сбор объявлений продажи (основной цикл, 30–120 мин) |
| `krisha-rental` | Сбор аренды (5–15 мин) |
| `krisha-geobind` | Геопривязка объявлений к ЖК/адресу/координатам |
| `krisha-viewcount` | Реальные просмотры (Playwright, цикл ~1 ч) |
| `krisha-homsters` | Обогащение первичного рынка (Homsters.kz, 5 дней) |
| `krisha-korter` | Обогащение (Korter.kz: класс ЖК, застройщик, 5 дней) |
| `krisha-homeportal` (timer) | Реестр КЖК: официальные данные (1 ЖК / 90 с) |
| `krisha-hype-news` (timer) | LLM-анализ новостей → хайп-локации (21:00) |
| `krisha-crime` (timer) | Преступность КПСиСУ: полная история Астаны + ежедневный инкремент (06:30) |
| `krisha-floorplan` (timer) | SigLIP-сканер планировок + мусорные фото |
| `krisha-market` | Рыночные справочные данные |
| `krisha-alerts` | Telegram-алерты (платные, Stars) |
| `krisha-sitebot` | Telegram-логин + дайджест избранного |

Cron сервера: `krisha_complex_scan.py` каждые 20 мин (карточки ЖК, щадящий
delay 120 с / batch 10), `street_audit_daily.py` в 04:30 (отвязка улиц).

### Кроны оркестратора (Hermes, UTC+5)

| Крон | Расписание | Назначение |
|------|------------|------------|
| `github-trending-digest` | 10:00 | Дайджест GitHub Trending + Radar (Threads) одним постом |
| `threads-githubradar-daily` | 09:00 | Сбор канала @githubradar (материал для 10:00, без доставки) |
| `hh-kz-vacancy-scan` | 9:00, 14:00 | Тихий скан вакансий hh.kz (IT-профиль) |
| `linkedin-threads-vacancy-scan` | 12:00, 18:00 | Скан IT-вакансий LinkedIn + Threads (лиды в vacancy_leads.json) |
| `hh-vacancy-digest` | 20:00 | Топ-3 вакансии hh.kz + лиды LinkedIn/Threads (с проверкой закрытых) |
| `hype-tracker-collect` | 8:00, 20:00 | Снимки хайпа (25 ресурсов) |
| `media-scan-5x` | 8/11/14/17/20 | Медиа-скан СМИ |
| `threads-hourly-scan` | каждый час | Упоминания недвижимости в Threads |
| `news-daily-collect` | 08:30 | Сбор новостей недвижимости |
| `hype-news-daily-report` | 21:00 | LLM-анализ новостей → рейтинги локаций + отчёт в чат |
| `hatuli-daily-backup` | 03:00 | Бэкап на Windows (C:\BU_HATULI, ротация 5) |

Автообновление из GitHub: `hatuli-update.timer` (каждые 5 мин, pull master +
рестарт web/apartments/rental; временно отключено). Ветка — `master`.

---

## Установка

```bash
# 1. Клонировать
git clone git@github.com:MelNikVl/hatuli.git
cd hatuli

# 2. Виртуальное окружение
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 3. PostgreSQL
sudo apt install postgresql
sudo -u postgres createuser -P krisha
sudo -u postgres createdb -O krisha krisha_bot
sudo -u postgres createdb -O krisha hype_tracker   # для хайп-аналитики
psql -U krisha -d krisha_bot -h localhost -f bot/db/schema.sql
# + миграции hype_tracker (таблицы hype_*, news_analysis_runs)

# 4. Конфигурация
cp .env.example .env
# Заполнить .env: DATABASE_URL, ADMIN_PASSWORD, DEEPSEEK_API_KEY и т.д.

# 5. Запустить сервисы
sudo cp *.service *.timer /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now krisha-web krisha-apartments krisha-rental \
    krisha-geobind krisha-viewcount krisha-homsters krisha-korter
```

---

## Переменные окружения (.env)

```env
DATABASE_URL=postgresql://krisha:***@localhost/krisha_bot
ADMIN_PASSWORD=               # Пароль веб-панели
DEEPSEEK_API_KEY=             # LLM-анализ новостей/хайпа
BOT_TOKEN=                    # Telegram-бот (легаси)
ADMIN_TELEGRAM_ID=            # ID владельца
PARSER_ENABLED=0              # 0 = сбор продаж выключен
AI_TEXT_ANALYSIS=0            # 1 = платный LLM-анализ текста
```

---

## Планы

- Личный кабинет пользователя (избранное, уведомления, привязка Telegram)
- Расширенный рейтинг ЖК (отзывы, тайный покупатель)
- Мониторинг ресурсов сервера в /admin/settings
- Дальнейшее автозаполнение характеристик ЖК (этажность из homeportal в
  housing_class_test, как сделано для квартир)
- SigLIP 2 / ViT-L-16 для более точной детекции планировок
