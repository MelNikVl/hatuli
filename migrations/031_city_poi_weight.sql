-- Вес точки притяжения для хайп-скоринга (см. hype_tracker) — пока не
-- используется в формуле (рейтинг = упоминания/максимум × 100, без веса),
-- заведён для будущей калибровки: не все точки одинаково значимы (Байтерек
-- vs небольшой ЖК-ориентир).
ALTER TABLE city_poi ADD COLUMN IF NOT EXISTS weight REAL NOT NULL DEFAULT 1.0;
