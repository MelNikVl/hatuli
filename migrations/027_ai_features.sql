-- Факты о ЖК, дополненные AI-слоем (bot/core/ai_text_analysis.py) из текстов
-- объявлений: локация, что рядом, архитектура, холл, охрана, консьерж,
-- закрытый двор, детская площадка, паркинг, закрытая территория.
-- Каждое поле хранит {"value":..., "source_listing_id":..., "source_url":..., "added_at":...}
-- Существующее поле никогда не перезаписывается — только дополняется новыми.
ALTER TABLE complexes ADD COLUMN IF NOT EXISTS ai_features JSONB;
