"""
Слой БАНКИ — ЗАГЛУШКА (всегда 0).

Замысел: оценивать, насколько объект «дружелюбен» к финансированию:
  - подходит ли под госпрограммы (7-20-25, Отбасы 5%, Наурыз);
  - разница рыночная ипотека vs льготная для этого объекта;
  - принимают ли банки такой объект в залог (год дома, статус).

Для реализации нужны: парсер ставок (НБРК / KDIF / Отбасы),
условия программ по типу объекта. Обсуждается отдельно.
"""
from __future__ import annotations


async def compute(listing: dict) -> tuple[int, str]:
    return 0, "слой банков — заглушка (в разработке)"
