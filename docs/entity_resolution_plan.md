# Entity resolution / data spine — план архитектуры

Статус: **фаза 1 (ЖК) реализована** — см. миграцию
[043_entity_resolution.sql](../migrations/043_entity_resolution.sql),
[bot/core/entity_resolution.py](../bot/core/entity_resolution.py),
интеграцию в [newbuild_common.py](../newbuild_common.py)
`ensure_complex()`, backfill
[backfill_entity_resolution.py](../backfill_entity_resolution.py) и
админ-страницу `/admin/entity-ids` ("АЙДИ"). **Фаза 2 (юниты) — только
план ниже, код не пишется**, пока фаза 1 не покажет уровень шума
авто-матчинга в проде (решение заказчика, 2026-08-12).

## Зачем

Сейчас у каждого источника (Крыша, Korter, Homsters, Homeportal.kz,
застройщики напрямую — BI Group/NAK/ORDA Invest/Bazis/Sensata/Свой дом/
Qadam) — свой ID объекта. Без единого постоянного ID на ЖК все обогащения
(фото, тех.характеристики, отзывы, аналитика) рассыпаны по несвязанным
записям, и нельзя достоверно сказать "это тот же дом".

## Что уже есть (не с нуля)

- `complexes.id` — уже фактически постоянный entity_id ЖК, к нему уже
  пришиты `complex_materials`, `complex_tech_specs`, `complex_reviews`,
  `newbuild_units`, избранное.
- Матчинг сейчас — **по нормализованному имени**: `ensure_complex()`
  ([newbuild_common.py](../newbuild_common.py)) апсертит по
  `lower(name)`; `apartment_listings.complex_name` тоже связывается с
  `complexes.name` через `lower(trim(...))` в SQL-джойнах по всему
  проекту (например [terminal_extras.py](../terminal_extras.py) —
  роут `/admin/complex/{id}`).
- Кросс-связи с источниками — **однослотовые колонки**, не таблица:
  `complexes.krisha_url`, `complexes.korter_url`,
  `complexes.newbuild_source` + `newbuild_source_id` (уникальны как пара,
  но один ЖК = максимум одна связь на источник), плюс
  `complexes.source_info JSONB` — свободный "мусорный ящик" под
  Korter/Homsters обогащение без схемы и без confidence/аудита.
- Ручная правка уже существует: `/admin/analytics/complexes?tab=complexes_fix`
  ([bot/admin_web.py:1080](../bot/admin_web.py)) — переназначение
  объявления на другой ЖК руками. Плюс аудит "мусорных" ЖК/улиц
  ([bot/core/complex_audit.py](../bot/core/complex_audit.py)).

## Слабые места текущей схемы

1. **Матчинг по имени — единственный сигнал.** Опечатка, ребрендинг
   застройщиком, два разных ЖК с одинаковым названием в разных районах —
   молча сливаются или расходятся, без объяснимости "почему".
2. **Один слот на источник.** Если у ЖК окажется два разных ID в одном
   источнике (например, старая и новая карточка на Крыше) — второй
   некуда записать, кроме как затереть первый.
3. **Нет confidence/аудита.** Нельзя посмотреть, когда и почему
   конкретная связь source→complex появилась, и откатить её отдельно от
   остальных.
4. **`source_info` без схемы** — каждый источник пишет туда что хочет,
   читающий код (`complex_detail` в terminal_extras.py) вручную
   разбирает вложенные ключи `korter`/`homsters` руками.

## Решение по ID (зафиксировано, реализовано)

- **PK / entity_id = `complexes.id` (bigint), без семантики.** Никаких
  `jk-nak-001`-подобных ключей с зашитым источником/застройщиком —
  застройщики ребрендятся, ЖК меняют владельцев, транслит плодит дубли.
  Семантика (кто застройщик, откуда пришли) живёт в атрибутах
  (`developer_id`, `complex_source_links.source`), не в ключе.
- **Человеко-читаемый код — отдельная колонка `complexes.code`**, формат
  `JK-000123` (генерируется из id, `bot/core/entity_resolution.generate_complex_code`).
  Мутируема (в теории можно перевыдать), не участвует ни в каком FK —
  чисто для отображения в UI/ссылках, не для связей между таблицами.

## Предлагаемая схема (фаза 1, только ЖК)

Новая таблица-связка, `complexes.id` остаётся единственным entity_id:

```sql
CREATE TABLE complex_source_links (
    id            SERIAL PRIMARY KEY,
    complex_id    INT NOT NULL REFERENCES complexes(id) ON DELETE CASCADE,
    source        TEXT NOT NULL,      -- 'krisha' | 'korter' | 'homsters' |
                                       -- 'homeportal' | 'bi_group' | 'nak' |
                                       -- 'orda_invest' | 'bazis' | 'sensata' |
                                       -- 'svoydom' | 'qadam' | ...
    source_id     TEXT NOT NULL,      -- id/slug объекта в системе источника
    url           TEXT,
    match_method  TEXT NOT NULL,      -- 'name_exact' | 'name_fuzzy+geo' |
                                       -- 'developer_source_id' | 'manual'
    confidence    NUMERIC,            -- 0..1, NULL для manual (=1 по факту)
    matched_at    TIMESTAMPTZ DEFAULT now(),
    matched_by    TEXT,               -- 'auto' | admin username
    UNIQUE (source, source_id)
);
CREATE INDEX ON complex_source_links (complex_id);
```

Один ЖК — много строк (по одной на источник, и даже несколько на один
источник, если понадобится). `complexes.krisha_url`/`korter_url`/
`newbuild_source(_id)`/`source_info` не трогаем сразу — оставляем как
read-through кэш ещё на один цикл, чтобы не ломать существующий код
разом; новые записи пишем в обе схемы, старые колонки выводим из
употребления отдельным шагом после того, как весь читающий код
переедет на `complex_source_links`.

## Матчинг: сигналы и уверенность

Скоринг вместо бинарного да/нет:

- **Точное совпадение нормализованного имени** (текущий метод) — базовый
  сигнал, `confidence` начинается с этого.
- **+ Гео-близость** (координаты источника, если есть, в пределах
  ~150 м от адреса/координат ЖК) — резко поднимает уверенность, ловит
  ребрендинг под новым именем и расходит однофамильцев.
- **+ Застройщик совпадает** (`developers` уже есть как справочник) —
  доп. сигнал.
- **Порог**: выше — авто-связка (`matched_by='auto'`); ниже — попадает в
  очередь на ручную проверку **в существующем UI**
  `/admin/analytics/complexes?tab=complexes_fix` (расширить его, а не
  строить новый инструмент — там уже есть паттерн "показать кандидат →
  подтвердить/отклонить").

## Миграция существующих данных

Разовый backfill-скрипт (в духе уже существующих
`map_csv_db.py`/`match_site_csv.py` в репозитории):
1. Из `complexes.krisha_url`/`korter_url` → строки с `match_method='manual'`
   (то, что уже стоит руками/историческим импортом, считаем подтверждённым).
2. Из `complexes.newbuild_source`+`newbuild_source_id` → строки с тем же
   методом, что использовался при первичном импорте (`ensure_complex`
   сейчас матчит по имени → `match_method='name_exact'`).
3. Из `complexes.source_info` (korter/homsters блоки) → распарсить
   вложенные `url`/`id`, если есть, иначе оставить как есть (эти два
   источника пока могут остаться на старой схеме, если у блоков нет id).

## Фаза 2 (юниты) — зафиксирована в плане, НЕ реализуется сейчас

Решение заказчика (2026-08-12): расписать сейчас, чтобы спайн фазы 1
сразу строился с прицелом на юниты, но не кодить, пока фаза 1 не
покажет уровень шума авто-матчинга в проде.

Тот же паттерн spine, что и у ЖК:

```sql
CREATE TABLE unit_source_links (
    id            SERIAL PRIMARY KEY,
    unit_id       INT NOT NULL REFERENCES newbuild_units(id) ON DELETE CASCADE,
    source        TEXT NOT NULL,
    source_id     TEXT NOT NULL,
    match_method  TEXT NOT NULL,
    confidence    NUMERIC,
    evidence      JSONB,        -- какие сигналы сработали и с каким весом —
                                 -- в отличие от complex_source_links.match_method
                                 -- (короткая строка), тут нужна разборка "что
                                 -- именно совпало", юнитов на порядок больше ЖК
    matched_at    TIMESTAMPTZ DEFAULT now(),
    matched_by    TEXT,
    UNIQUE (source, source_id)
);
```

entity_id = `newbuild_units.id` (тот же принцип: PK без семантики, как у
ЖК). Что в `evidence` и как считается матчинг:

**Блокировка** (сужаем пространство кандидатов до матчинга, иначе
N×M сравнений юнитов внутри ЖК): по `(complex_id, корпус/секция,
комнатность)` — сравниваем только юнитов внутри одного блока.

**Сигналы внутри блока** (скоринг, не бинарно):
- Этаж — высокий вес (точное совпадение почти всегда значит один и тот
  же юнит в пределах блока).
- Метраж — с допуском ±2–3 м² (разные источники округляют по-разному);
  хранить оба значения — «как в источнике» и нормализованное, не
  затирать одно другим.
- Номер квартиры, если парсится из URL/текста источника — почти
  золотой ключ (сразу высокая confidence при совпадении).
- Фингерпринт описания (хэш нормализованного текста) — копипаст между
  площадками при перепубликации одного и того же юнита случается
  часто и это бесплатный сигнал (не нужен внешний API).
- Фото: pHash на каждое фото объявления, пересечение хэшей — сильный
  сигнал; НО фото, встречающееся в 5+ разных объявлениях — считаем
  stock/рендер застройщика и исключаем из матчинга (иначе склеит
  вообще всё с одинаковой обложкой ЖК). Планировка (schematic) —
  стабильнее интерьерных фото, если различима от них — отдельный,
  более надёжный сигнал.
- Вспомогательные (низкий вес поодиночке, но усиливают совпадение):
  цена ±5%, телефон подавшего объявление (если есть в обоих
  источниках), пересечение периодов активности (first_seen/last_seen
  не могут не пересекаться, если это правда один и тот же юнит).

**Пороги** — те же, что в фазе 1 (`AUTO_MATCH_THRESHOLD`/
`REVIEW_QUEUE_THRESHOLD` из `entity_resolution.py`, переиспользуем как
константы, не заводим отдельные). **Review queue** — тот же
существующий UI (`/admin/analytics/complexes?tab=complexes_fix`),
расширить под юниты, а не строить новый инструмент.

## 2026-08-13: Фаза 2 — методология по итогам фазы 1 (гейт условие проверено)

Условие запуска фазы 2 (см. выше) — "план расписан, код не пишется,
пока фаза 1 не покажет уровень шума авто-матчинга в проде". Фаза 1
отработала в проде весь день 2026-08-12/13 (spine, гейт-мердж, review-UX,
adaptive recheck) — условие проверяемо, не гипотетически.

### Что фаза 1 реально показала про "уровень шума"

Не абстрактно — конкретные числа из живых прогонов:
- **Наивный OR-критерий (гео/developer/address) без перекрёстных
  проверок — ~25%+ реальных ошибок на первых же кейсах.** Гейт-10
  транслит-мерджа поймал 1 живую ошибку из 10 (10%) сразу, а по итогам
  полного разбора — 5 отдельных классов багов (phase-conflict без
  транслита, двухходовая split-lineage, отсутствие словаря-синонима
  "позиция", неограниченный developer-mismatch, непереименованный
  после расшивки паспорт), каждый нашёлся ТОЛЬКО через ручной спот-чек
  реальных результатов, не через код-ревью заранее.
- **После итеративных гвардов (5 предохранителей) — 0 ошибок на 120
  мерджах**, подтверждено спот-чеком 10 случайных пар + assert
  "unravel_involved не тронут" + повторный sweep (199→91 групп, все
  26 оставшихся "высокая уверенность по гео" объяснены явной причиной).
- **Гипотезы о структуре сигнала регулярно опровергались фактом**, не
  подтверждались: "hot=новое объявление" (58% архиваций у объявлений
  старше 14 дней, не у свежих), "первая очередь = без номера везде"
  (implicit-phase-1 понадобился отдельным фиксом), "адрес всегда
  доступен" (address_match не сработал НИ РАЗУ на 54 krisha spine-
  строках — `complexes.address` пуст структурно).

**Вывод методологии**: сигналы для фазы 2 нельзя спроектировать
заранее и один раз откалибровать — план ниже (веса/пороги) это
ГИПОТЕЗА для первого гейта, не финальная конфигурация. Дисциплина
фазы 1 (гейт 10 → спот-чек → массово; provenance-lineage предохранитель;
review-UX ДО массовых решений, не после) переносится в фазу 2 как
обязательная, не опциональная практика.

### Гейт перед гейтом: данные для матчинга физически не там, где ожидалось

Проверил по факту (не по плану) — где вообще есть overlap для матчинга
юнитов:

| источник | юнитов | ЖК | фото |
|---|---|---|---|
| bi_group | 7988 | 58 | 0 |
| sensata | 1959 | 11 | 0 |
| bazis | 1548 | 18 | 0 |
| svoydom | 1318 | 14 | 0 |
| nak | 262 | 2 | 0 |
| orda_invest | 39 | 3 | 0 |

**Комплексов с юнитами от 2+ источников (newbuild_units × newbuild_units)
— 0.** Каждый источник покрывает СВОИ ЖК, пересечения между
developer-прямыми источниками нет вовсе — план фазы 2 (сравнение
`unit_source_links` между source'ами newbuild_units) сегодня решал бы
задачу с НУЛЕВЫМ количеством реальных пар. Фото — 0 у ВСЕХ источников
(`photos` пуст) — pHash-сигнал из плана нереализуем без отдельного
сбора фото, которого сейчас нет вообще.

**Реальный overlap — в другом месте**: `apartment_listings` (обычные
объявления Крыши от физлиц/риелторов, часто перепродающих застройщика)
с `is_new_build=TRUE` — 363 строки, из них **29 ЖК** пересекаются с
`newbuild_units` по `complex_id` (через `complexes.name`). Это и есть
настоящая задача дедупликации юнитов — "то же самое жильё, что
застройщик продаёт напрямую (newbuild_units) и что переоформлено на
Крыше от риелтора (apartment_listings)" — не "три застройщика продают
один ЖК" (такого не бывает структурно).

### Пересмотр плана под факт

1. **Блокировка** остаётся `(complex_id, комнатность)` — но сравниваем
   `newbuild_units` (сторона застройщика) против `apartment_listings
   WHERE is_new_build=TRUE` (сторона Крыши), не units×units.
   `building`/`section` есть только у стороны застройщика — Крыша
   такого не парсит, сигнал одноsторонний, участвует только когда
   доступен с обеих сторон (тот же принцип, что address_match).
2. **Фото/pHash — не в первый гейт.** Нет данных вовсе (0 фото у
   newbuild_units), а собирать их специально под гипотетический сигнал
   — работа отдельного масштаба (обход фото застройщиков), не часть
   алгоритма матчинга. Отложено, не отменено — тот же паттерн, что
   address-backfill для фазы 1 (голодающий сигнал — сначала данные,
   потом вес).
3. **Первый гейт — те же 29 ЖК, не вся база.** Малый, проверяемый
   вручную набор (десятки-сотни юнитов, не тысячи) — гейт 10 →
   спот-чек → массово по ТОЙ ЖЕ дисциплине, что транслит-мердж.
   Signals для первого прохода: этаж (точное совпадение), метраж
   (±2-3м²), цена (±5%), пересечение first_seen/last_seen — все
   уже собираются, ничего нового не нужно тянуть с источников.
4. **`unit_duplicate_candidates`, не `unit_source_links` для review** —
   тот же паттерн, что `complex_duplicate_candidates` (эта задача —
   дубль СУЩНОСТИ между источниками одного типа данных, не привязка
   "источник→ЖК"), review-UX по тому же шаблону (evidence инлайн,
   bulk-eligible фильтр) сразу, не после первого прогона.
5. **provenance-lineage предохранитель заранее**, не постфактум — если
   юнит когда-либо разбирался вручную (одобрен/отклонён в review), это
   должно блокировать повторное авто-решение так же, как
   `unravel_involved_ids()` блокирует транслит-мердж для тронутых
   расшивкой id.

### Numbers-first проход — реальные 28 ЖК, до кода

(Уточнение: комплексов с overlap реально 28, не 29 — округление в
предыдущем проходе.) Блокировка `(complex_id, комнатность)` между
`newbuild_units` (застройщик) и `apartment_listings WHERE is_new_build`
(Крыша) на всех 28 ЖК:

| шаг | пар | % от блокировки |
|---|---|---|
| блокировка (complex_id, комнатность) | **1710** | 100% |
| + этаж точное совпадение | 151 | 8.8% |
| + метраж ±3м² (оба сигнала) | **75** | 4.4% |
| + цена ±5% (доп. к этажу+метражу) | 32 | 1.9% (42.7% от 75) |
| + пересечение first_seen/last_seen (доп. к этажу+метражу) | 26 | 1.5% (34.7% от 75) |
| этаж+метраж, подтверждено ЦЕНОЙ ИЛИ ДАТОЙ | 45 | 2.6% (**60%** от 75) |
| этаж+метраж, **БЕЗ подтверждения ни одним** | **30** | 1.8% (**40%** от 75) |

**Вывод**: этаж+метраж сам по себе даёт правдоподобный кандидат-пул
(75 из 1710, 95.6% отсева блокировки) — но **40% этих кандидатов (30
пар) не подтверждаются ни ценой, ни пересечением дат активности вовсе**.
Дома с повторяющейся планировкой (тот же этаж, та же площадь на разных
секциях/подъездах) — реальный источник ложных совпадений именно на
этой паре сигналов, тот же класс риска, что naive OR-критерий в
транслит-мердже фазы 1 (sibling-sweep урок: geo+developer одни, без
перекрёстной проверки, дали 6/7 ложных auto). Цена/дата не опциональные
"вспомогательные" сигналы из исходного плана — они закрывают
структурную дыру, которую этаж+метраж не закрывают сами.

**Решение для первого гейта фазы 2** (когда до него дойдёт): auto —
этаж+метраж И (цена ИЛИ дата), то есть ровно тот 60%-ный "45" —
остальные 30 (этаж+метраж без подтверждения) — в review
(`unit_duplicate_candidates`), не auto и не молчаливый пропуск. Ни
одного пропущенного нб/al без пары данных (price_missing=0 на этой
выборке) — в отличие от фазы 1, здесь цена доступна с обеих сторон
всегда, сигнал не голодает.

Дальше — реализация ждёт отдельного гейта (не начата).

## Что НЕ входит ни в одну из фаз (пока)

- Звонки и сделки — источника данных для них в проекте пока нет вообще,
  писать схему под них преждевременно.

## Статус решений

Все три пункта, которые раньше тут стояли открытыми — закрыты и
реализованы:
1. Пороги confidence: **≥0.8 авто, 0.5–0.8 очередь, <0.5 связь не
   создаётся** — константы в `entity_resolution.py`.
2. Источники фазы 1 — весь список из `complex_source_links.source`
   выше, ничего не урезано.
3. Старые колонки (`krisha_url`/`korter_url`/`newbuild_source(_id)`)
   остаются read-through кэшем — не тронуты, `complex_source_links`
   пишется параллельно.

Открытое — только по фазе 2 (см. выше): когда фаза 1 отработает в
проде и будет видно распределение confidence по реальным юнитам,
пересмотреть веса сигналов (сейчас они не откалиброваны на данных,
только по интуиции из плана).

## 2026-08-12: homeportal подключён к spine, первые данные калибровки

`hype_tracker/homeportal_scan.py` (таймер `krisha-homeportal`, раз в
~45 мин) раньше писал найденного кандидата напрямую в
`homeportal_objects.matched_complex_id`, мимо `complex_source_links` —
585 связей копились вне spine, backfill гасил разово, но каждый
следующий прогон таймера открывал ту же дыру заново. Теперь кандидата
всё ещё ищем локально (дёшево, без похода в БД на каждый из ~600
объектов реестра — точное/нормализованное/fuzzy/first_word имя), но
итоговые confidence/match_method считает `score_match()` (имя через
pg_trgm по **нормализованным** именам — сырые "ЖК "Х" (очередь N)"
почти не похожи как триграммы на "Х", сигнал имени гас бы почти везде
без normalize) + гео/застройщик БИН/адрес, и пишет `record_source_link()`
— auto (≥0.8) сразу в spine, иначе в очередь. Производные записи
(`homeportal_objects.matched_complex_id` для UI, `housing_class_test`,
`complexes.description`, `complex_tech_specs.developer_bin`) теперь
льются только при `auto` — раньше лились для ЛЮБОГО найденного
кандидата, включая самый слабый tier (`first_word`).

Источники, уже подтверждённые в spine (`source='homeportal'`),
пере-скору на каждый прогон не подвергаются (short-circuit по
`source_id`) — иначе расхождение точных координат homeportal с
Nominatim-геокодингом complexes (наблюдалось 300+ м на реальной паре)
роняет геосигнал и заново кладёт уже решённую пару в очередь review
каждые 45 минут.

Сухой прогон на 60 случайных уже сматченных ранее объектах (пере-скор
"как будто впервые видим", без записи, см. диагностику при ревью) —
первые реальные данные калибровки, которых план выше просил:
**35 auto / 24 review / 1 skip**. Разумное распределение, но нашёлся
конкретный ложноположительный кандидат на auto: `ЖК "Darmen 2"` (0.80,
`name_fuzzy(0.64)+geo+developer`) matched на существующий `Darmen 1` —
разные очереди одного проекта у одного застройщика в 150 м друг от
друга проходят по гео+застройщику даже при слабом имени.

## 2026-08-12: сигнал токена очереди/фазы (фикс Darmen-ловушки)

Добавлен сигнал токена очереди/фазы
(`bot.core.entity_resolution._phase_token`, вызывается из `score_match`
через новые опциональные `name_a_full`/`name_b_full` — СЫРЫЕ имена
нужны отдельно от чищенных для trigram, потому что "Дармен (2 очередь)"
после `norm_name()` теряет скобки вместе с номером). Парсит «N-я
очередь», «очередь N», хвостовой номер, хвостовые римские цифры
(валидируются round-trip'ом, отсекает случайные слова вроде "MIX").
Правило: у обеих сторон номер есть и он разный → потолок confidence
`0.79` (не даёт уйти в auto, но не гасит остальные сигналы — решение
просто уходит человеку); равный → небольшой бонус `+0.1`; номер только
с одной стороны → нейтрально, не трогаем.

Три ручных прогона `score_match()` (без БД, с одинаковым гео+
застройщиком, разными именами) подтвердили ровно то поведение, которое
и требовалось:
- `Darmen 2` ↔ `Darmen 1` → **0.79, review** (было 0.80 auto).
- `Darmen 2` ↔ `Darmen (2 очередь)` → **0.98, auto** (номер совпал —
  бонус, а не потеря сигнала из-за скобок).
- `Darmen` ↔ `Darmen 2` → **0.88, auto**, без капа (номер только с
  одной стороны — не мешаем).

Повторная холостая калибровка тех же 60 объектов (те же `object_id`,
не новая случайная выборка) с сигналом фазы: **34 auto / 25 review / 1
skip** — авто просело всего на 1 из 60, и это именно `Darmen 2`↔`Darmen
1` (заслуженно). Два уже-review случая (`MoD.Comfort-1`↔`Mod Comfort-3`,
`Highvill Park-1`↔`Highvill Park 2`) остались review — там cap не бил
(и без него confidence уже был ниже 0.79).

## 2026-08-12: sibling-sweep на живом newbuild-прогоне — второй фикс (implicit phase 1)

Живой прогон `newbuild_weekly.py` (все 5 импортёров, rc=0, 0 ошибок,
612 с) сам по себе не добавил новых spine-записей — реестр не изменился
за несколько часов между автоматическим `Persistent=true` докатом
таймера (машина была выключена в момент понедельничного триггера,
докатился сегодня в 13:55–15:13, ДО этого ручного прогона) и ручным
запуском в 20:30: все 94 ЖК уже были в spine, `ON CONFLICT DO NOTHING`.
Это же и стало досрочной проверкой идемпотентности (п.5 плана) — два
прогона подряд, ноль новых строк, ноль дублей кандидатов
(`UNIQUE(source, source_id, complex_id)` + `DO UPDATE`, не растёт).

Единственная свежая активность — 21 review-кандидат (18 bazis + 3
orda_invest), все на 0.75 (`name_exact+developer`). Причина —
структурная, не шум: `bazis_import.py`/`orda_invest_import.py` вообще
не отдают geo/адрес (`address=None` в коде), поэтому даже 100%-точное
имя не наберёт 0.8 никогда. Топ-10 review 0.7–0.79 (п.3 плана) — ровно
эти записи, все «очевидно те же» ЖК. Конфликтов — 0 за всё время по
всем 5 источникам.

Дальше — sibling-pair sweep (один застройщик, ≤150 м, схожие имена) по
всей базе ЖК новостроек нашёл 7 реальных пар вида «база + номер фазы»
и **6 из 7 всё ещё проходили в auto** несмотря на фикс токена фазы
выше:

```
GAUHARTAS      vs Gauhartas 2      -> 0.91 auto  (WOULD MERGE)
Nur Aspan      vs Nur Aspan 2      -> 0.91 auto  (WOULD MERGE)
Махаббат       vs Махаббат-2       -> 0.90 auto  (WOULD MERGE)
Акерке         vs Акерке-2         -> 0.88 auto  (WOULD MERGE)
Отан           vs Отан 2           -> 0.84 auto  (WOULD MERGE)
Inju Arena     vs Inju Arena 2     -> 0.91 auto  (WOULD MERGE)
Asylym Park 1  vs Asylym Park 2    -> 0.79 review (единственная, где номер
                                       был с ОБЕИХ сторон — только её и
                                       ловило правило "с одной стороны ->
                                       нейтрально")
```

Причина: правило "номер только с одной стороны → нейтрально" (буквально
заданное правило, дало верный результат на синтетическом
`Darmen`↔`Darmen 2` → без капа) не покрывает доминирующий в реальных
данных паттерн — **первую очередь почти никогда не подписывают номером
вовсе** ("Nur Aspan"/"Nur Aspan 2", а не "Nur Aspan 1"/"Nur Aspan 2").
У безномерной стороны токена нет вовсе -> нейтрально -> в auto её
тащат геосигнал+застройщик, ровно как когда-то тащили Darmen.

Подтверждено дополнительно прямо в БД: `Landmark Gold` (id 2451) и
`Landmark Gold, V очередь` (id 4103) — уже отдельные строки
`complexes`; `Поколение Бизнес` (3966) и `Поколение Бизнес 2` (1688) —
тоже отдельные. Т.е. для этих самых застройщиков в базе УЖЕ есть
прецедент "фаза = отдельный complex_id" — auto-слияние безномерной
стороны с номерованной было бы реальной ошибкой, не гипотетической.

**Фикс:** `_phase_token()` теперь возвращает `(номер, база_без_суффикса)`.
Когда номер есть только с одной стороны — сравниваем "голую" сторону с
базой номерованной (той же строкой без суффикса фазы). Совпадают
(similarity >= 0.8) → голая сторона это неявная фаза "1", дальше как
обычно (равно → бонус, не равно → потолок 0.79). Не совпадают →
цифра, похоже, не про фазу вообще — нейтрально, как раньше.

Это меняет результат зафиксированного ранее теста: `Darmen` ↔
`Darmen 2` **больше не "auto без капа"**, теперь `0.79, review`
(`phase_mismatch(1~implicit!=2)`) — сознательное решение после того,
как реальные данные (6 из 7 sibling-пар) показали, что старое правило
пропускало именно этот, самый частый в проде паттерн.

Повторные проверки после второго фикса:
- Все 7/7 sibling-пар из живой базы → **review**, ноль `WOULD MERGE`.
- Холостая калибровка тех же 60 homeportal-объектов: **29 auto / 30
  review / 1 skip** (было 34/25/1 после первого фикса, 35/24/1 совсем
  без фиксов). Просадка авто заметнее (35→29, не 35→34) — но при
  разборе каждая из 6 новых demotions объяснима тем же паттерном
  (явный номер очереди у источника против безномерной "базовой" записи
  в `complexes`, например `ЖК "LANDMARK GOLD" (6 очередь)` →
  `Landmark Gold` — и это ЛЕГИТИМНЫЙ повод для review, не шум: у
  Landmark Gold в базе уже есть отдельная строка под конкретную
  очередь, слияние "по умолчанию" было бы реальной ошибкой того же
  типа, что и Darmen). Ни одной demotion без объяснимой причины.

## 2026-08-12: регрессионный тест-файл нашёл третий баг (`_clean_base`)

`tests/test_entity_resolution_phase.py` — 10 тестов на реальных данных
из прод-БД (не выдуманные координаты/БИН): 3 кейса Дармена (homeportal
object_id=1266 `ЖК "Darmen 2"` против complexes `Darmen 1` (id=2972) и
`DARMEN` (id=2053, реальный безномерной ЖК на той же площадке) +
1 конструированный вариант с номером в скобках) и 7 sibling-пар из
sweep (parametrize).

Первый прогон тестов упал на `Darmen 2` (сырое имя `ЖК "Darmen 2"`) vs
`DARMEN` (уже чистое имя без приставки) — implicit-phase-1 не сработал
вовсе (`conf=1.0`, никакого `phase_*` в method). Причина: "база"
номерованной стороны считалась из СЫРОГО имени (`_phase_token()`
намеренно работает на сыром тексте — иначе теряется номер в скобках,
см. фикс от того же дня выше) и после вычитания суффикса фазы
оставалась `'жк "darmen'` — с приставкой "ЖК" и одинарной кавычкой
внутри. trgm similarity `'жк "darmen'` к чистому `'darmen'` — низкая
(шум, не реальное несовпадение), implicit-phase-1 condition
(`base_sim >= 0.8`) не срабатывал, сигнал молчал.

**Фикс:** `_clean_base()` — стрипает приставку "жк"/"мжк" и обёрточные
кавычки из базы перед сравнением (тот же класс нормализации, что
`homeportal_scan.norm_name()` делает для всего имени, только точечно
для базы после вычитания суффикса фазы).

Это не косметика — пересчёт всей 60-объектной калибровки после фикса:
**21 auto / 38 review / 1 skip** (было 29/30/1 ДО `_clean_base`). Баг
маскировал implicit-phase-1 почти везде, где у источника сырое имя с
приставкой "ЖК \"...\"" (то есть почти всегда у homeportal) — после
фикса сигнал наконец видит эти пары. Разобрал все 21 оставшихся
`phase_*` в methods — каждая: явный номер очереди у источника
(`Flagman - 2`, `Aisar - 4`, `Sezim Qala - 8`, `Поколение Бизнес
(очередь 2/3)`, ...) против безномерной "базовой" записи в
`complexes` — легитимный повод для review, ни одного случая без
явного номера в исходном имени.

Финальная таблица калибровки (та же 60-объектная выборка, все версии
для сравнения):

| версия | auto | review | skip |
|---|---|---|---|
| без сигнала фазы | 35 | 24 | 1 |
| + explicit phase cap (Darmen-фикс) | 34 | 25 | 1 |
| + implicit phase 1 | 29 | 30 | 1 |
| + `_clean_base` (финал) | **21** | **38** | 1 |

Просадка авто с 35 до 21 (40%) — намного больше, чем изначально
предполагалось ("не должно просесть заметно"), но не шум: это
структурный признак реальных данных (номер очереди у источника vs
безномерная база в `complexes` — доминирующий паттерн у homeportal) и
каждая demotion лично разобрана и объяснима. Плата — заметно больше
объектов в review-очереди при первом же боевом прогоне homeportal
после лендинга; корректность важнее объёма.

## 2026-08-12: буквенные блоки (Family Nest — тот же паттерн, литерой вместо номера)

Пред-валидация `gap_sweep_krisha_korter.py` (--test, dry) нашла живой
пример НОВОГО паттерна: `Family Nest F` (Крыша) → `Family Nest`
(наш, безномерной) — **0.81 auto**, хотя в базе уже отдельные строки
под буквенный блок: `Family Nest F | Bi Group` (id=3996), `Family
Nest - F Блок` (id=3235), `Family Nest F` (id=2481). `_phase_token()`
ловил только цифры/римские — букву как признак фазы не видел вовсе.

**Фикс:** `_phase_token()` теперь извлекает и буквенный блок/корпус —
"F блок"/"блок F"/"корпус F"/"литера F" (маркер-слово, регистр буквы
не важен) и хвостовую ОДИНОЧНУЮ букву без маркера ("Family Nest F") —
для последней регистр решает: заглавная в СЫРОМ (не lower-нутом)
тексте похожа на блок, строчная — почти наверняка обычный конец слова
("Paris" не даёт ложный токен, "GAUHARTAS" тоже — трейлинг-буква не
своим словом, часть "GAUHARTAS" целиком). Токен вида `block:f` — та же
структурная логика сравнения (равны → бонус, разные → потолок), но с
важным отличием от чисел: implicit-default есть только у ЧИСЕЛ ("1" —
первая очередь почти всегда неявная); у БУКВ такого дефолта нет —
голая сторона без буквы не может "означать" конкретный блок без
подтверждения, поэтому для букв implicit-ветка всегда даёт потолок,
никогда бонус (`Family Nest` ↔ `Family Nest F` → `0.79 review`,
`phase_mismatch(?~implicit!=block:f)`).

3 новых регресс-теста (`tests/test_entity_resolution_phase.py`) на
реальных `id=3743`/`id=2481`: безномерная база vs буква → review; тот
же блок с двух источников → auto (бонус); разные буквы → review.
Холостая калибровка тех же 60 объектов: **20 auto / 39 review / 1
skip** (было 21/38/1) — минус 1, единственная новая demotion
(`Sensata Park (блоки А, В)` → `Sensata Park`, было 1.00 auto) —
легитимна: источник явно называет МНОЖЕСТВЕННОЕ число блоков, не один
конкретный.

Повторный sibling-sweep (один застройщик, ≤150 м, sim≥0.5) с фильтром
"буквенный токен хотя бы с одной стороны" нашёл 2 пары, **1 всё ещё
WOULD MERGE**: `ЖК Highvill Ishim D` ↔ `ЖК Highvill Gold Ishim` —
**но это НЕ провал буквенного фикса**: implicit-ветка не сработала
вовсе (в methods нет `phase`/`block` — база "highvill ishim" и полное
имя "highvill gold ishim" недостаточно похожи, `base_sim < 0.8`,
сигнал корректно промолчал). 0.84 auto тут целиком от
`name_fuzzy(0.72)+geo+developer` — независимый от токена фазы риск
(общие слова в имени + geo ≤150 м + тот же застройщик у явно разных
продуктовых линеек Highvill). Отдельная проблема, не тема этой
калибровки — зафиксирована как открытый вопрос, не чинилась.

## 2026-08-12: политика гранулярности ЖК (расшивка blob-комплексов)

Найдено при разборке blob-аудита: адресное расхождение между точками
одного `complex_id` — не всегда ошибка матчинга. Многие крупные
проекты (`Городской Романс`, `AUSTRIA`, `Millennium Park`, ...) имеют
по многу пятен/кварталов строительных разрешений с формально разными
привязками к улице — это ОДИН проект, просто административно нарезан
на участки. Настоящая ошибка — Family Nest A/B/F, UIA.BIRLIK A-E — где
источник сам явно и однозначно называет каждую часть отдельным именем
(буква/номер блока или очереди), это разные маркетинговые сущности.

**Правило:** комплекс = проект одного permit-ареала (одна стройка,
один застройщик, один этап согласования, даже если формально несколько
участков/пятен). Отдельный `complex_id` — только когда источник сам
явно и однозначно нумерует/литерует части (очередь/блок/корпус с
единичным явным значением на КАЖДОЙ сравниваемой стороне). "Пятно N"/
"квартал N" (пермитная нарезка участка, не маркетинговая единица) и
перечисления номеров ("блоки 1, 2, 3", "пятна 6, 9" — про диапазон, не
про конкретный блок) — никогда не повод для разделения, весь диапазон
остаётся одним ЖК.

### Правила кластеризации для расшивки

1. Явный токен фазы/блока с ОБЕИХ сторон, разный → **split** (разные
   complex_id).
2. Токен явный только с одной стороны, "голая" база совпадает с базой
   номерованной (implicit phase 1, см. `_phase_token`/`score_match`) →
   **split** — тот же принцип, что и у самого сигнала фазы в score_match.
3. "Пятно N"/"квартал N"/перечисление номеров ("1, 2, 3") → **никогда
   не split**, весь диапазон один ЖК (`_phase_token` теперь явно
   возвращает `None` для этих случаев, см. `_PYATNO_KVARTAL_RE`/
   `_ENUM_LIST_RE`).
4. Без явных токенов вовсе, но доверенные (не закарантиненные) адреса
   расходятся И реальное гео > 1 км → **review**, не автоматическое
   решение — человек смотрит руками.
5. Продуктовые токены (Gold/Premium/Comfort и т.п. — не фаза/блок, а
   линейка продукта) в кластеризации НЕ участвуют — их обрабатывает
   отдельный Highvill-пенальти (follow-up, ещё не реализован).

### Таблица известных вердиктов (регресс-тесты `_phase_token`)

| ЖК | вердикт | почему |
|---|---|---|
| `Family Nest A`/`B`/`F` | **split** | явные буквенные блоки, разные адреса |
| `Городской Романс` (пятна 6/8/9/...) | **один ЖК** | пятна одного permit-ареала |
| `AUSTRIA` (блоки 1,2,3 / 4,11 / ...) | **один ЖК** | перечисление номеров, не конкретный блок |
| `arupark` / `AruPark 2-1,2-2,3` | **split** | явные номера, база "arupark" совпадает |
| `Parkland` {1,2,C,D} / {E,F} | **split по адресу** | разные улицы (ул. С902 vs ул. Бейбарыс Сұлтан) при явных, но неоднозначно группируемых токенах — адрес решает группировку внутри "split" |

## 2026-08-12: гейт 2 — интеграционный проход (перескор/калибровка/дрейф)

Задача-список после приёмки гейта 2 (карантин координат + unravel_blobs.py
+ финальный address_match()). Скрипты: [rescore_spine_auto.py](../rescore_spine_auto.py),
[calibrate_homeportal_dry.py](../calibrate_homeportal_dry.py),
[address_match_drift_20260812.py](../address_match_drift_20260812.py).

**1) Перескор spine auto-связей.** Реальный продукт `score_match()` в
spine на момент прохода — 55 строк (`matched_by='auto'`): 54 krisha
(`gap_sweep_krisha_korter.py`, Stage C) + 1 bi_group (`seed_source`,
не продукт `score_match()`, пропущен). Homeportal — 0 живых auto-строк
(все 585 либо `legacy_import`/`backfill_2026-08-12` (confidence=1.0
захардкожен, не score_match), либо `manual`/`unravel` (сплиты
`unravel_blobs.py`) — рескорить нечего, "без плацебо-гео" здесь
неприменимо к текущим данным (карантин уже зануляет lat/lon прямо в
колонках, код-путь для будущих homeportal-auto строк в скрипте есть и
проверен на пустом множестве). Перескор 54 krisha-строк (живой
ре-фетч сохранённых krisha URL за адресом — geo/имя/застройщик не
дрейфуют у реального объекта, ре-фетчить их ради этой задачи не
нужно): **0 изменений** — не потому что address_match() ничего не
делает, а потому что `complexes.address` пустой у ВСЕХ 54 (структурная
дыра данных, не баг скоринга: адрес для этих ЖК никогда не заполнялся
`krisha_complex_import.py`). Демоций — 0.

**2) Холостая калибровка + регресс.** Регресс-сюит: 34 теста прошли
без изменений; добавил 2 новых (`test_parkland_district_and_uchastok_
are_noise_not_signal`, `test_same_street_still_matches_after_noise_fix`
в `tests/test_entity_resolution_granularity.py`) — фикс `address_match()`
(коммит `2ff574b`) сам был БЕЗ регресс-теста до этого прохода, дыра
закрыта на реальной паре PARKLAND 2/Parkland-E из homeportal_objects.
60-объектная dry-калибровка: детерминированная выборка (ORDER BY
object_id ASC LIMIT 60 среди уже сматченных) даёт **29 auto / 27
review / 4 skip** — конкретные id из прошлых раундов (21/38/1) никогда
не сохранялись в репозиторий, так что это НЕ дрейф той же популяции, а
новая, но воспроизводимая база для будущего сравнения. Точечная
атрибуция самого кода (`address_match_drift_20260812.py`, старый
шум-лист vs новый на тех же 60 парах, где есть оба адреса): **0
флипов** на этой выборке (12 пар — оба-True, 1 — оба-False/None) — фикс
подтверждён напрямую на реальной Parkland-паре (True→False), но не
задевает эту конкретную выборку статистически; редкий, но реальный
edge case, не системный сдвиг.

**3) Не решено в этом проходе.** Цифры "52→82" из задачи не
реконструировались ни из логов, ни из БД (ближайшие кандидаты —
review-очередь 79/99 записей, либо 52 новых complex_id от
`unravel_blobs.py` (50 пасс1 + 2 пасс2) против 56 фактических
`создан complex #` в логах) — не совпадает чисто ни с одной гипотезой,
переспросил у заказчика вместо угадывания на объекте про целостность
данных.

**4) Дрейф display-кэша homeportal.** Найдено ровно 2 расхождения
`homeportal_objects.matched_complex_id` (кэш) vs `complex_source_links`
(spine) — оба object_id (1170 'Aisar - 4', 1175 'Tumar Residence')
матчились ЛОКАЛЬНЫМ вейтлифтом в 14:18-14:26 today, ДО коммита
`7ecc31e` (20:29, "писать матчи в spine через record_source_link()") —
старый код писал `matched_complex_id` для люБОГО найденного кандидата
без проверки против spine, и с тех пор объекты не переобрабатывались
(инкрементальное окно `fetched_at < 7 дней` их не трогает). За это
время появились НОВЫЕ дублирующие complex_id того же реального ЖК
(3938 'Aisar' от bi_group-импорта, 3155 'Tumar Residence' от
korter-импорта) — старый локальный индекс подхватил их вместо уже
подтверждённых в spine (125 'ЖК Aisar', 3953 'Tumar Residence —
Идеальный Вар...'). Исправлено точечно: оба `matched_complex_id`
переставлены на значение spine (`match_method='reconciled_to_spine_
2026-08-12'`). Сами дубли-complex_id (125↔3938, 3953↔3155) НЕ слиты —
отдельная задача того же типа, что уже делали для Family Nest
(`merge_family_nest_dups.py`), не в объёме этого прохода.

Заодно нашлись 2 живых случая "[object_id]-в-имени" (дизамбигуация
коллизии имени в `unravel_blobs.py`, см. код у `seed_name = f"{seed_name}
[{cl[0]['object_id']}]"` — сырой internal id в user-facing названии
ЖК) — переименованы на человекочитаемый разделитель по адресу:
`#4344` `... [29]` → `... (блок 3, Хусейн бен Талал)`; `#4346`
`... [671]` → `... (позиция 12, уч. 38)` (у #4346 разделитель по улице
не годился — та же "Е117", что и у оригинала #4302, тот случай сам по
себе подозрителен на реальный дубль, а не два разных ЖК — вынесен как
кандидат на спот-чек в шаге 3, не решался здесь).

**Follow-up (зафиксирован, не реализован в этом проходе):**
- **Кэш читает из spine, не отдельная колонка.** `homeportal_objects.
  matched_complex_id` — ровно тот тип write-through кэша, что уже дал
  дрейф дважды (585 связей мимо spine до `7ecc31e`, и это 2-объектное
  расхождение после) — правильный конечный вид: UI (`homeportal_
  monitor.html`, `_parser_homeportal.html`) читает `complex_source_links
  WHERE source='homeportal'` через JOIN, колонка `matched_complex_id`
  либо удаляется, либо остаётся чисто денормализованным индексом,
  который переписывается ИЗ spine (never наоборот).
- **Уличные алиасы.** `address_match()` сравнивает токены улицы буквально
  — не знает, что "Мангилик Ел"/"Мәңгілік Ел", "Е117"/"Е 117" и
  подобные варианты написания/раскладки — одна и та же улица. Нужен
  словарь алиасов (или хотя бы normalize пробелов вокруг букв индекса
  улиц типа "Е117"), отдельная таблица или константа рядом с
  `_ADDR_NOISE`, наполняется по мере находок (тот же паттерн, что и сам
  `_ADDR_NOISE` — по факту, не заранее).
- **Ручное переименование `[object_id]`-случаев.** Дизамбигуация по
  сырому id — аварийный fallback, не решение: политика на будущее —
  либо дизамбигуировать сразу по значимому токену (адрес/блок), как
  сделано вручную для #4344/#4346 выше, либо (если и адреса совпадают)
  роутить в review вместо тихого создания дубля с id в имени.

## 2026-08-12: дельта 52→82 — сверка (истинная тройка + спот-чек 5)

Оба числа задачи — из прошлого (несохранённого в репозиторий) отчёта
("Total: 52 splits" / финальное "82 new complexes"). Сверка с
БД/логами: **52 подтверждается** (auto-split-комплексов: 50 пасс1 + 2
пасс2 — `unravel_blobs_20260812.log`/`unravel_blobs_pass2_20260812.log`,
"ИТОГ: auto-split-комплексов=N"), **82 не подтверждается ничем** —
ближайшее реальное число это 56 (см. ниже), 82 не сходится ни с одной
проверенной метрикой, действительно похоже на опечатку/помесь чисел из
другого отчёта. Истинная тройка (SQL, не пересчёт из логов):

| метрика | запрос | значение |
|---|---|---|
| split-паспортов (то самое "52") | `grep -c "SPLIT auto" \| ИТОГ auto-split-комплексов` (оба лога) | 50 + 2 = **52** |
| реально созданных complex-строк | `grep -c "создан complex #"` (оба лога), сверено с `SELECT count(*) FROM complexes WHERE id > 4283` (56 из лога + 4 несвязанных, от других процессов того же дня) | **56** |
| связей homeportal перенесено в новые complex_id | `SELECT count(*) FROM complex_source_links WHERE source='homeportal' AND matched_by='unravel'` | **110** |
| review-routed (разных паспортов, не решено автоматически) | Salzburg #2218, Austria #2399 — те же 2 в обоих логах (пасс2 просто переспросил нерешённое, не нашёл новых) | **2** |
| коллизий имени (`[object_id]`-дизамбигуация) | `grep -c "коллизия имени"` | 0 (пасс1) + 2 (пасс2) = **2**, оба переименованы вручную выше |

**56 vs 52 — гипотеза из задачи ("4 INSERT из упавшей попытки до фикса
[object_id]") НЕ подтвердилась и неверна по конструкции**: PostgreSQL
откатывает INSERT целиком при нарушении unique constraint — упавшая
попытка физически не может оставить в таблице частичную/висячую
строку, а сам код (`unravel_blobs.py`) ловит именно это исключение и
тут же делает ВТОРОЙ, успешный INSERT с дизамбигуированным именем — то
есть на каждую коллизию всё равно ровно 1 финальная строка, не 2 и не
0. **Реальная причина** — прямо в логе: 4 родителя в пасс1 разошлись
не на 1, а на **2 новых кластера каждый** (`SPLIT auto (2 новых
кластеров)`: `#1866 Altyn Saulet`, `#2094 Besterek`, + 2 других) — 46
родителей × 1 кластер + 4 родителя × 2 кластера = 54 строки в пасс1,
+ 2 в пасс2 = 56, при 52 паспортах. Чистая арифметика "1 паспорт = 1
новый complex" была неверным допущением задачи, не багом кода.

### Спот-чек 5 новых splits

1. **#125 'ЖК Aisar' → #4262 'Aisar 3'** (явный номер "3", в БД уже
   было "Aisar" (безномерное, implicit-1) как паспорт) — split
   корректен сам по себе, НО вскрыл более широкую проблему: `Aisar`
   теперь размазан по МИНИМУМ трём complex_id (125 паспорт, 3938
   'Aisar' от bi_group-импорта — см. дрейф кэша в разделе выше, 4262
   'Aisar 3' этот split) — кандидат на отдельную консолидацию, не
   исправлено здесь.
2. **#142 'arupark' → #4263** (группа из 3 разных номеров "2-2"/"3"/
   "2-1" в ОДИН новый кластер, а не 3) — корректно: все три делят
   фактически один адрес (два написания того же перекрёстка), явно
   разошлись только с безномерной базой "AruPark", не друг с другом —
   ровно как задокументировано в `test_arupark_numbers_split`.
3. **#1866 'Altyn Saulet' → #4277 + #4278** (2 новых кластера, явные
   номера очередей 1/2/3 у трёх сторон, все разные) — чистый, образцовый
   случай правила 1 (явный токен с обеих/всех сторон, разный → split).
4. **#4076 'MOD Urban' → #4313** — ПОГРАНИЧНЫЙ: три РАЗНЫХ явных номера
   очереди (2, 3, 4) слиты в ОДИН новый комплекс, не в три. Формально
   похоже на нарушение правила 1, но оправдано правилом 5 (адрес решает
   группировку) — все три делят один адресный след
   (ул. Т. Рысқұлова, уч. 18 / уч. 18/5, соседние варианты одного
   участка) и политикой "один permit-ареал = один ЖК". Технически
   консистентно с зафиксированной политикой, но именно такой случай
   (явно разные номера продаются как отдельные позиции застройщиком) —
   кандидат на пересмотр политики, не бага; отмечено, не исправлено.
5. **#4302/#4346 'Bagystan'** (+ ранее существующий #3132 'Bagystan
   (позиция 2, 4)') — НАСТОЯЩАЯ находка спот-чека, требует ручного
   разбора: "Bagystan (очередь 3/4)" (паспорт #4302, адрес голый "ул.
   Е117", без участка) и "Bagystan (позиция 12)" (новый #4346, "ул. Е
   117, уч. 38") разошлись как split, потому что токен "позиция 12"
   извлёкся через generic хвостовой номер-фоллбэк (не через словарь
   "очередь", "позиция" туда не входит) — разная категория токена,
   формально "не равны", но неизвестно, действительно ли "позиция" и
   "очередь" — одна и та же ось нумерации у ЭТОГО конкретного
   застройщика. Плюс уже существующий #3132 на том же участке (д.39-42,
   рядом с уч.38) — итого 3 отдельных complex_id на одной улице у
   одного проекта. НЕ трогал руками — маршрутизирован сюда как
   кандидат на review, не на автоматическое решение.

## 2026-08-12: транслитерация, Highvill-пенальти, Tandau

Задача гейта 2, п.5. Реализовано в `bot/core/entity_resolution.py`:

**Транслитерация** — `transliterate()`, посимвольная кириллица→латиница
(рус.+каз. буквы, приближённая, не ГОСТ). В `score_match()`: если
прямой `name_similarity()` не дотягивает до `FUZZY_NAME_THRESHOLD`,
пробуем ещё раз на транслитерированных обеих сторонах (no-op на уже
латинском тексте, безопасно гонять без детекции алфавита) — если так
лучше, используем и помечаем `_translit` в имени тира (`name_exact_
translit`/`name_fuzzy(N)_translit`). Найдено на живой паре: id=2217
'Tandau' (латиница) / id=2563 'Тандау' (кириллица), ~25 м друг от друга
— pg_trgm без транслита similarity=0, сигнал имени гас бы полностью.
Слито `merge_tandau_dups.py` (канон = больше данных, id=2217, 14
объявл./2 source_links против 1/0 у 2563).

**"Highvill-пенальти" (продуктовый токен)** — `_product_token()` +
блок в `score_match()`, тот же структурный класс сигнала, что
`_phase_token()`/потолок confidence, но БЕЗ implicit-ветки (нет
"неявной линейки продукта по умолчанию", в отличие от "неявной первой
фазы/очереди" у номеров — токен на одной стороне при отсутствии на
другой это тоже потолок, не нейтрально). Список токенов: gold/premium/
comfort/lux/deluxe/business/smart/standard/plus/elite/vip/prime/grand.
Закрывает открытую находку калибровки 2026-08-12: `Highvill Ishim D`
vs `Highvill Gold Ishim` было 0.84 auto (`name_fuzzy+geo+developer`,
ни одного `phase_*` в methods) — теперь `0.79, review,
product_mismatch(None!=gold)`. Второй живой повод — id=2315 'Comfort
Tandau' при разборе Tandau/Тандау: НЕ транслит-дубль (в неск. км от
2217/2563, реально другой адрес), 'Comfort' — продуктовый токен;
дополнительно естественно защищён и слабым name_fuzzy (полное имя
"comfort tandau" против голого "tandau" не дотягивает даже до
`FUZZY_NAME_THRESHOLD` без токена вообще — двойная защита, не только
пенальти).

**Регресс:** 4 новых теста в `tests/test_entity_resolution_phase.py`
(translit auto-match на реальной Tandau-паре, translit no-op на
несвязанных именах, Highvill product-mismatch → review, совпадающий
токен → бонус) + 2 в `test_entity_resolution_granularity.py` (address_
match noise-фикс, см. выше) — итого регресс-сюит **40/40**. Повторная
холостая калибровка (`calibrate_homeportal_dry.py --n 60`) после обоих
новых сигналов: **29/27/4, без изменений** относительно версии до
сигналов — ожидаемо, ни транслит-, ни продуктовые пары не попали в эту
конкретную детерминированную выборку 60 (сигналы узко специализированы,
не должны массово шевелить основную популяцию).

**Sweep по скрипт-вариантам дублей** (`sweep_translit_dups.py`,
группировка ВСЕХ 2238 живых complexes по `transliterate(norm_name(name))`)
нашёл, что Tandau/Тандау — не единичный случай, а системный паттерн:
**199 ключей с >1 complex_id**, из них **114** с высокой уверенностью
(ровно 2 id, гео ≤150 м, почти везде тот же застройщик) и **85**,
требующих ручного разбора (нет гео с одной/обеих сторон, >2 id в
группе, или гео >150 м — среди них и сама уже известная находка
дрейфа кэша `Aisar`/`Tumar Residence`, и `Highvill Gold Ishim`
дважды). **Это не смёржено** — 114+ пар одним скриптом без
поштучной проверки это на порядок больше блеста, чем Family Nest
(2 пары) или Tandau (1 пара) до этого, и найдены минимум два ложных
паттерна прямо в выдаче (`samruk towers` — developer_match=False,
скорее совпадение звучания, не дубль; `komfort`/`birlik`/`saranda` и
похожие короткие общеупотребимые слова в "ручной" корзине на самом
деле, скорее всего, просто разные ЖК со случайно похожим коротким
именем — короткие ключи менее надёжны). Список сохранён в
`sweep_translit_dups_20260812.log` — кандидат на отдельную задачу
слияния с тем же уровнем проверки, что Tandau/Family Nest, не на
массовый auto-merge.

## 2026-08-12: массовый транслит-мердж — гейт нашёл 5 живых багов, итог

Задача-продолжение sweep'а выше: критерий auto-мерджа зафиксирован
заказчиком — translit-имена равны И (гео≤150м ИЛИ тот же застройщик
ИЛИ address_match), выживает — у кого больше данных
(links×2+listings+enrichment), продуктовый токен на любой стороне —
всегда review. `merge_translit_dups.py` + `migrations/047_complex_
duplicate_candidates.sql` (review-очередь под пары complex_id↔complex_id,
параллельно `complex_source_link_candidates`, у той другая природа
кандидата).

**Гейт (--limit 10, реальная запись) сработал как задуман** — спот-чек
десятки нашёл, что #4272 «ЖК "Abai Joly" (3 очередь)» тихо слился
обратно в базовую «Abai Joly», отменив легитимный split того же дня.
Откатил, дальше по цепочке нашёл ещё 4 варианта той же болезни —
каждый закрыт своим предохранителем, каждый подтверждён регресс-тестом
на реальных данных (`tests/test_merge_translit_dups.py`, 15 тестов):

1. **`phase_conflict()`** — реюз `_phase_token()`/score_match-логики: явный
   токен с обеих сторон разный, ИЛИ implicit-phase-1 против явного N —
   конфликт. Закрыл Abai Joly.
2. **`split_provenance_conflict()`** — тот же гейт нашёл ВТОРОЙ баг
   («Времена Года»: #311→#4266→#4339, два хопа, «(Лето)» между базой и
   суффиксом сбивало pg_trgm-сходство ниже 0.8) — ходит по ВСЕЙ цепочке
   `provenance→split_from`, не только по прямому родителю.
3. **Транслит в самом `phase_conflict()`** — ТРЕТИЙ/ЧЕТВЁРТЫЙ баги
   (Altyn Saulet/Ауен): implicit-phase-1 сравнение без транслита между
   кириллицей и латиницей давало сходство ~0, конфликт молчал.
4. **«позиция» — синоним «очереди»** в `entity_resolution.py`
   (Bagystan): отсутствовал в словаре токена фазы, generic хвостовой
   фоллбэк вместо номера позиции цеплял номер участка из адреса.
5. **`unravel_involved_ids()`** — ПЯТЫЙ, самый грубый и надёжный:
   паспорт #1866 после расшивки НЕ переименован (`complexes.name`
   осталась голой «ЖК Altyn Saulet»), хотя по факту это 3-я очередь —
   любой id, тронутый сегодняшней расшивкой (родитель ИЛИ ребёнок),
   исключён из auto-мерджа translit-движком целиком, до ручной сверки
   имён отдельной задачей.

**Второй раунд, уже по прямому указанию заказчика:**
`developer_conflict()` — явное расхождение застройщика (оба значения
присутствуны, разные) даунгрейдит auto в review, даже если гео≤150м
само по себе достаточно. Нашли на живой паре `samruk towers` (#230,
BAUR DEVELOPMENT GROUP) / `Самрук Towers` (#435, ТОО «Berkut
Development») — 93 м друг от друга, прошли бы в auto чисто по гео.

### Массовый прогон и результат

`--limit 1000`, реальная запись: **111 кластеров** (128 complex_id
слито в канон) + **9** из гейта (Abai Joly откачен) = **120 всего за
день**. **137 пар** ушли в `complex_duplicate_candidates` (review):
61 split_provenance_conflict, 57 no_confirming_signal, 12
product_token_mismatch, 6 phase_conflict, 1 developer_conflict
(samruk towers).

**Верификация (по чек-листу заказчика):**
- Повторный `sweep_translit_dups.py`: было 199 групп → **91** (108
  закрыто). Из оставшихся «высокая уверенность по гео» (26, старая
  бинарная метрика sweep'а, не знает про phase/product/developer-
  гварды) — **все 26 объяснены** конкретной причиной в `complex_
  duplicate_candidates` (по большей части — явный номер
  очереди/блока у одной стороны, продуктовый токен, или unravel-
  involved), ни одного необъяснённого пропуска.
- Спот-чек 10 случайных смёрженных пар: дубль `is_garbage=TRUE`,
  канон жив, 0 сиротских `complex_source_links`/`complex_favorites`,
  0 `apartment_listings` на старом имени — по всем 10 чисто.
- `complex_favorites` (PK `user_id, complex_id`) — таблицу изначально
  пропустил в скрипте (переносил только `apartment_listings` и
  `complex_source_links`), заказчик поймал в чек-листе ДО того, как
  это стало реальной потерей данных — 0 избранных было у смёрженных
  дублей на момент прогона (повезло), но код теперь переносит и его
  (`DELETE` дубль-строки, если канон уже в избранном у того же
  `user_id`, иначе `UPDATE`).
- Assert «ноль unravel_involved id тронуто»: **1** случай нашёлся —
  #3938 «Aisar» → #125 «ЖК Aisar», но это ГЕЙТ-1 (03:19, ДО того как
  `unravel_involved_ids()` вообще появился в коде) и #125 involved
  только как родитель #4262 «Aisar 3» (разной, явно 3-й очереди,
  которую этот мердж не задевает вовсе — #3938 сама никогда не была
  частью расшивки). Проверено руками, мердж корректен. Массовый
  прогон (гвард уже был активен) — реально 0.

### Samruk Towers — review-карточка (для недельного ритуала)

`complex_duplicate_candidates` id для пары (230, 435), `reason=
'developer_conflict'`:

| | #230 'samruk towers' | #435 'Самрук Towers' |
|---|---|---|
| застройщик | BAUR DEVELOPMENT GROUP (id=210) | ТОО «Berkut Development» (id=11) |
| адрес | — (пусто) | Астана, Есильский р-н, ул. Кайыма Мухамедханова, 17 |
| источники | — | krisha (`legacy_import`), homeportal #97 (`legacy_import`) |
| гео | 51.141636, 71.38653 | 51.140884, 71.38712 |

Гео-дельта — **93 м**. Имя идентично после транслитерации, но
застройщик расходится по ОБЕИМ сторонам явно (не "нет данных с одной
стороны" — тот случай сигнал просто не участвует, тут именно
расхождение). Варианта два: (а) реально два разных здания в 93 м друг
от друга случайно с одинаковым именем — конкурирующие проекты рядом
не редкость; (б) ошибка в `developer_id` на одной из сторон (легенда
источника перепутана при импорте). Без похода на источник (сайт
застройщика/реестр) решить нельзя — оставлено человеку.

### Follow-up (в дополнение к списку выше)

- **`complexes.address` пуст структурно на krisha-стороне.** Найдено
  ещё в п.1 (перескор spine auto-связей) — все 54 krisha-строки имели
  `address IS NULL`, address-сигнал не мог участвовать вообще ни разу.
  `krisha_complex_import.py` умеет парсить адрес с детальной страницы
  (`COALESCE`, не перезаписывает), но, похоже, не для всех уже
  привязанных ЖК запускался/донёс данные. Нужен целевой backfill-проход
  (`krisha_complex_import.py` уже пишет `source_info->'krisha'.address`
  — проверить, не расходится ли он с `complexes.address`, и почему
  COALESCE не сработал) — иначе address-сигнал (15% веса в score_match)
  голодает на самом крупном источнике (1163 krisha-связи в spine).
- **Гео-уточнение при auto-матче.** Когда источник (homeportal — точные
  координаты объекта; krisha_complex_import — координаты со страницы
  ЖК) даёт гео точнее/надёжнее, чем текущее `complexes.lat/lon`
  (обычно Nominatim-геокодинг по адресу, менее точный) — на auto-матче
  стоит обновлять `complexes.lat/lon` из источника (COALESCE-подобно,
  не затирая руками поправленное), не только читать координаты для
  сравнения и выбрасывать. Сейчас `score_match()` гео только СРАВНИВАЕТ,
  никогда не улучшает то, с чем сравнивает в следующий раз.
- **Обогащение bazis/orda_invest координатами/адресом.**
  `bazis_import.py`/`orda_invest_import.py` не отдают geo/адрес вообще
  (`address=None` в коде, см. "sibling-sweep на живом newbuild-прогоне"
  выше) — структурная причина, почему их review-кандидаты (21 запись,
  0.75 `name_exact+developer`) никогда не наберут auto: не то что
  сигнал слабый, а сигналов физически два из четырёх возможных. Нужно
  либо парсить гео/адрес с детальных страниц источников (если они там
  есть), либо re-geocode по адресу, если источник хотя бы текстовый
  адрес отдаёт где-то ещё.
