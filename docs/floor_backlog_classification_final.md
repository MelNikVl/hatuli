# Floor-backlog classification (all 1590) — FINAL

Read-only классификация всех 1590 записей floor-бэкфилл-бэклога
(`floor IS NULL AND NOT EXISTS property_listings`), follow-up п.3.
**Завершена полностью.** Ни одной реальной floor-записи не сделано —
только GET-запросы (тот же UA, что боевой парсер) + локальный JSONL,
БД не тронута.

## Итог

```
1590 / 1590 обработано, 0 ошибок, 0 блокировок, 0 неоднозначных

flat_layout:  772  (48.6%)  — карточка типа квартиры ЖК, этаж СТРУКТУРНО неприменим
normal:       767  (48.2%)  — обычное объявление конкретной квартиры
unavailable:   51  ( 3.2%)  — страница мертва/редиректит на главную ("Крыша" в <title>)
```

**Пересчёт vs предыдущие оценки**: canary по умолчанию (id ASC,
20 старейших) дал 100% flat_layout — сильно смещённая выборка. Отдельная
несмещённая случайная выборка (15 шт., см. `docs/coverage_gaps_followup.md`-
соседний аудит) дала 40%. **Полная перепись (эта)** — 48.6% flat_layout,
ближе к случайной оценке, чем к id-ASC срезу — подтверждает находку:
`flat_layout` НЕ равномерно размазан, а концентрируется в СТАРОЙ части
id-диапазона, но не ТАК экстремально (100%), как показал самый первый
(смещённый) canary.

## Практический вывод для реального backfill-прогона (не выполнен)

Реально адресуемая для floor-заполнения популяция — **767 из 1590
(48.2%)**, не весь бэклог. Из них, по данным пост-CI dry-run (PR #17,
уже смерженного): `--order newest` даёт **50% реального успеха**
(10/20 filled), `--order random` — ~17% (2/12), `--order oldest` — 0%
(на конкретной выборке 10, но там уже все flat_layout были заранее
отфильтрованы отбором, то есть даже СРЕДИ настоящих квартир самая старая
часть почти не восстановима — вероятно, устарели/архивны).
**Рекомендация**: реальный прогон (после отдельного ОК) начинать с
`--order newest`, наибольший ожидаемый выход на затраченный HTTP-бюджет.

## Гейты — подтверждено

- Реальных floor-writes за весь этот follow-up — **0**.
- `--order newest/random/--seed` — уже в `master` (PR #17), протестировано.
- Полная классификация — read-only, ничего в БД не изменено.
