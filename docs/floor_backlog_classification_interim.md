# Floor-backlog classification (all 1590) — interim status

Read-only классификация всех 1590 записей floor-бэкфилл-бэклога
(`sell.flat_layout` / обычная квартира / недоступна / другое), запущена
в фоне для follow-up п.3 — **ещё выполняется на момент этого коммита**,
не блокирует PR (сам код-фикс `backfill_listing_floors.py` — уже в
`master`, PR #17 — полностью независим от того, закончена ли эта
классификация; она нужна только для точных финальных counts).

## Текущий прогресс (проверено напрямую — процесс жив, лог свежий, не завис)

```
867 / 1590 обработано (55%)
  flat_layout: 676  (78%)
  normal:      183  (21%)
  unavailable:   8  (1%)
```

Соотношение стабильно смещено к `flat_layout` в старой (по возрастанию
id, порядок по умолчанию скрипта) части бэклога — согласуется с
находками `docs/coverage_gaps_followup.md`/дневным аудитом ("самые
старые id систематически перекошены к flat_layout").

Процесс `classify_floor_backlog.py` продолжает работать (тот же
8-15с/запрос темп, что боевой `backfill_listing_floors.py`, намеренно не
ускорен). Итоговые числа по всем 1590 — отдельным follow-up сообщением
после завершения (ожидаемо ещё ~2-2.5 часа при текущем темпе). Реальных
floor-writes по-прежнему НЕ было ни одной — вся классификация read-only
(GET-запросы, JSONL-файл на диске, ничего не пишет в БД).
