# Telegram bot
aiogram==3.12.0

# Database
aiosqlite==0.20.0

# HTTP client (for parser and image hashing)
httpx==0.27.0

# HTML parsing (existing parser)
beautifulsoup4==4.12.3

# Image perceptual hashing
imagehash==4.3.1
Pillow==10.4.0

# Scheduling (Phase 2)
APScheduler==3.10.4

# AI (Phase 3)
anthropic==0.34.2

# Config
python-dotenv==1.0.1

# Admin web (existing)
fastapi==0.115.0
uvicorn==0.30.6
jinja2==3.1.4
python-multipart==0.0.9

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0

# PostgreSQL (основная БД — без этого не запустится вообще ничего)
asyncpg==0.29.0

# Google Sheets sync
gspread==6.1.2
google-auth==2.34.0

# Homsters.kz enrichment: обходит anti-bot защиту (403 без этого)
curl_cffi==0.7.1
psutil==7.2.2
